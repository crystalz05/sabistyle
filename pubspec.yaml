name: sabistyle
description: "A flutter ecomerce application"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  dartz: ^0.10.1
  get_it: ^9.2.1
  go_router: ^17.1.0
  dio: ^5.9.2
  shared_preferences: ^2.5.4
  google_fonts: ^8.0.2

  supabase_flutter: ^2.8.0
  app_links: ^7.0.0
  rxdart: ^0.28.0
  cached_network_image: ^3.4.1
  connectivity_plus: ^7.0.0
  webview_flutter: ^4.10.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

